# ACL — ตารางสิทธิ์ผู้ใช้ LeaveEasy

ตารางนี้สรุปจาก `leaveeasy-spec.md` หัวข้อ 2 — ใช้เป็นข้อมูลอ้างอิงตอนเขียน Firestore Security Rules รายห้อง (สัปดาห์ที่ 8)

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **พนักงาน** (`employee`) | ยื่นใบขอลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | เปลี่ยนสถานะใบลา (ของตัวเองหรือของคนอื่น) · ดูใบลาของพนักงานคนอื่น · แก้ไข/เพิ่ม/ลบประเภทการลา · ลบใบลาที่ไม่ใช่ของตัวเอง |
| ✅ **หัวหน้า** (`manager`) | ดูใบลาทุกใบของทุกคน · เปลี่ยนสถานะใบลา (อนุมัติ / ไม่อนุมัติ) · เขียนความเห็นในทุกใบ | ลบใบลาของคนอื่น · แก้ไข/เพิ่ม/ลบประเภทการลา |
| 🧾 **ฝ่ายบุคคล** (`hr`) | ทำได้ทุกอย่างที่หัวหน้าทำได้ (ดูทุกใบ · เปลี่ยนสถานะ · เขียนความเห็น) · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม/แก้/ลบประเภทการลา · เห็นแดชบอร์ดสรุปทั้งระบบ | — (ไม่มีข้อจำกัดเพิ่มเติมจากหัวหน้า) |

## หมายเหตุสำคัญ

- ค่าที่เก็บจริงในช่อง `role` ของ Firestore ใช้รหัสภาษาอังกฤษเสมอ (`employee` / `manager` / `hr`) ส่วนชื่อบทบาทภาษาไทยในตารางนี้ใช้แสดงผลเท่านั้น
- **ตอนนี้ปุ่ม/เมนู/การมองเห็นข้อมูลบนหน้าจอถูกจำกัดให้ตรงตามตารางนี้แล้ว** (ดูรายการด้านล่าง) — แต่เป็นการจำกัด**ฝั่งหน้าจอ (client-side) เท่านั้น** ไม่ใช่การบังคับที่ระดับฐานข้อมูล
- **`firestore.rules` ยังไม่บังคับตามบทบาท** — กฎที่มีผลจริงตอนนี้คือ "ต้องล็อกอินก่อน ถึงจะอ่าน/เขียนข้อมูลได้" เท่านั้น (ไม่สนใจ `role`) ดังนั้นถ้ามีคนแก้ JavaScript ในเบราว์เซอร์ตัวเอง หรือยิง request ตรงเข้า Firestore เอง ก็ยังข้ามข้อจำกัดในตารางนี้ได้อยู่ดี — การบังคับจริงระดับฐานข้อมูลเป็นงานของสัปดาห์ที่ 8 (ดู `leaveeasy-spec.md` หัวข้อ 8)

## สิ่งที่ถูกจำกัดไว้แล้วในโค้ด (client-side)

| จุดที่จำกัด | เงื่อนไข |
|---|---|
| เมนู "ประเภทการลา" (`js/nav.js`) | แสดงเฉพาะ role `hr` |
| หน้า `leave-types.html` (`js/leave-types.js`) | เข้าตรงผ่าน URL ได้ทุก role แต่ถ้าไม่ใช่ `hr` จะเห็นข้อความ "ไม่มีสิทธิ์" แทนฟอร์ม/ตาราง |
| รายการใบลา (`js/leave-requests.js`) | role `employee` เห็นเฉพาะใบของตัวเอง (`requesterId` ตรงกับ uid) · `manager`/`hr` เห็นทุกใบ |
| หน้ารายละเอียดใบลา (`js/leave-request-detail.js`) | role `employee` เปิดใบของคนอื่นไม่ได้ (ขึ้น "ไม่มีสิทธิ์") · ปุ่มอนุมัติ/ไม่อนุมัติ แสดงเฉพาะ role `manager`/`hr` |

## สิ่งที่ในตารางยังไม่มี UI ให้จำกัด (ยังไม่ใช่ลืมทำ)

- **ลบใบลาของตัวเอง** — ปุ่มลบยังไม่เคยถูกสร้างในโค้ดเลย
- **กำหนดผู้อนุมัติให้แต่ละใบ** — ยังไม่มี UI ส่วนนี้
- **แดชบอร์ดสรุปทั้งระบบ** — `dashboard.html` ยังไม่ถูกสร้าง (เป็นงาน Module 3 ตามสเปก)
