# CLAUDE.md

ไฟล์นี้ให้คำแนะนำแก่ Claude Code (claude.ai/code) เวลาทำงานกับโค้ดในโปรเจกต์นี้

## ภาษา

ตอบผู้ใช้เป็น **ภาษาไทยเป็นหลัก** ส่วนโค้ด ชื่อตัวแปร และรูปแบบ commit ให้คงไว้ตามที่วางไว้แล้วในโปรเจกต์ (ดูหัวข้อ "ชื่อตัวแปรเป็นภาษาไทย" ด้านล่าง) — ข้อนี้พูดถึงภาษาที่ใช้คุยเท่านั้น ไม่เกี่ยวกับตัวโค้ด

## โปรเจกต์นี้คืออะไร

LeaveEasy (🔧 ระบบขอลาออนไลน์) เป็นโปรเจกต์ของผู้เรียนในหลักสูตร ADT-RAISE Non-Degree Batch 2, Module 2 (สัปดาห์ที่ 6–9) เป็นเว็บ prototype ที่เขียนด้วย HTML/CSS/JS ธรรมดา (ไม่มี framework ไม่มีขั้นตอน build ไม่มีเซิร์ฟเวอร์ที่เขียนเอง) คุยกับ Firebase ตรง ๆ ผู้เรียนจะเพิ่มความสามารถทีละชั้นในแต่ละสัปดาห์ **ห้ามทำงานของสัปดาห์ถัดไปล่วงหน้า** แม้จะดูเหมือนเป็นขั้นต่อไปที่ชัดเจนก็ตาม

**`leaveeasy-spec.md` คือสเปกหลักที่ต้องยึดตาม** — อ่านไฟล์นี้ก่อนแก้พฤติกรรมใด ๆ ของระบบ มันกำหนดขอบเขตของแต่ละสัปดาห์ (หัวข้อ 8) สิ่งที่อยู่นอกขอบเขตของทั้งโมดูล (หัวข้อ 9) ชื่อช่องข้อมูล/ตัวพิมพ์เล็ก-ใหญ่ที่ถูกต้องบน Firestore และกฎการเปลี่ยนสถานะ ถ้าคำสั่งที่ได้รับขัดกับสเปก ให้ทักท้วงและถามก่อนลงมือเขียนโค้ด — อย่าเพิ่มฟีเจอร์ที่ไม่ได้ระบุ หรือดึงงานของสัปดาห์ถัดไปมาทำก่อนโดยไม่บอก

ความคืบหน้าปัจจุบัน: สัปดาห์ที่ 6→7 (ดู git log / README เพื่อดูสถานะล่าสุด) — การ**อ่าน**ข้อมูลจาก Firestore ต่อกับ `leaveRequests` แล้ว การ**สร้าง**ใบลาใหม่ (หน้า `new-leave-request.html`), การ**เปลี่ยนสถานะ**และการ**ส่งความเห็น**ในหน้ารายละเอียด (หน้า `leave-request-detail.html`) เขียนลง Firestore จริงแล้วทั้งหมด ส่วนการจัดการประเภทการลา (`leave-types.html`) ยังทำแค่ในหน่วยความจำ (ยังไม่เขียนกลับ Firestore) ซึ่งจะเป็นงานถัดไป

## คำสั่งที่ใช้บ่อย

- `npm run dev` — เปิดเว็บที่ `http://localhost:3000` ผ่านแพ็กเกจ `serve` (`serve -l 3000 .`) โปรเจกต์นี้ไม่มีคำสั่ง build, lint หรือ test
- `serve.json` ตั้ง `"cleanUrls": false` ไว้โดยเจตนา — ค่าเริ่มต้นของ `serve` จะ redirect `xxx.html` ไปเป็น `xxx` แล้ว**ตัด query string ทิ้ง** ซึ่งจะทำให้ลิงก์แบบ `leave-request-detail.html?id=...` ที่ใช้ทั่วทั้งโปรเจกต์พังทันที (เปิดได้แต่ไม่มี id) ห้ามลบการตั้งค่านี้ออก
- เปิดหน้าเว็บตรงจากไฟล์ (`file://`) ได้เช่นกัน — นี่คือเหตุผลที่โหลด Firebase ผ่าน SDK แบบ `-compat` ด้วย `<script>` ธรรมดา แทนที่จะใช้ ES modules (`type="module"` ถูกเบราว์เซอร์บล็อกบน `file://`)

## สถาปัตยกรรม

**หนึ่งไฟล์ HTML ต่อหนึ่งหน้าจอ** แต่ละหน้าดึง CSS/JS ที่ใช้ร่วมกันเข้ามาใช้:
- `index.html` — หน้าแรก/รวมลิงก์ (ไม่มี JS ของตัวเองนอกจากแถบเมนู)
- `leave-requests.html` + `js/leave-requests.js` — รายการใบลาทั้งหมด (ตาราง)
- `new-leave-request.html` + `js/new-leave-request.js` — ฟอร์มยื่นใบลา
- `leave-request-detail.html` + `js/leave-request-detail.js` — รายละเอียดใบลาหนึ่งใบ, การเปลี่ยนสถานะ, ความเห็น
- `leave-types.html` + `js/leave-types.js` — จัดการประเภทการลา (เพิ่ม/แก้/ลบ)
- `seed.html` + `js/seed.js` — ปุ่มใช้ครั้งเดียวสำหรับใส่ข้อมูลตัวอย่างจาก `js/data.js` ลง Firestore

โค้ด JS ที่ใช้ร่วมกัน โหลดโดยทุกหน้าผ่าน `<script>` (ไม่มี bundler ไม่มี import):
- `js/firebase-config.js` — ตั้งค่าการเชื่อมต่อโปรเจกต์ Firebase (`leaveeasy-puttaraksa`) ต้องโหลดหลัง SDK compat ของ firebase-app/firestore และก่อนสคริปต์หน้าไหนก็ตามที่เรียกใช้ `db`
- `js/nav.js` — วาดแถบเมนูด้านบนลงใน `<div id="nav"></div>` แก้เมนูที่ไฟล์นี้ที่เดียว ทุกหน้าเปลี่ยนตาม
- `js/util.js` — ตัวช่วยเล็ก ๆ ที่ใช้ทุกหน้า: `esc()` (แปลงข้อความให้ปลอดภัยก่อนใส่ใน innerHTML), `ป้ายสถานะ()` (สร้าง span ป้ายสถานะ), `เวลาตอนนี้()` (เวลาปัจจุบันในรูปแบบ `ปี-เดือน-วัน ชั่วโมง:นาที`), `ค่าจากURL()` (อ่านค่าพารามิเตอร์จาก URL)
- `js/data.js` — ข้อมูลตัวอย่างที่พิมพ์ค้างไว้ (`window.LEAVE_DATA`) ชื่อช่องข้อมูลตรงกับโครงสร้างบน Firestore เป๊ะ ใช้เติมรายการเลื่อนลงของประเภทการลาและเป็นต้นทางของข้อมูล seed กำลังถูกทยอยเลิกใช้ทีละหน้าเมื่อการอ่าน/เขียน Firestore จริงพร้อมมากขึ้น

**ชื่อตัวแปรเป็นภาษาไทย** ชื่อฟังก์ชันและตัวแปรตลอดทั้งโค้ด JS (`ใบ`, `ความเห็น`, `เปลี่ยนสถานะ` ฯลฯ) เป็นคำภาษาไทยโดยตั้งใจ ตามสเปกที่กำหนดให้หน้าจอ/โค้ดบรรยายเป็นภาษาไทย ส่วนชื่อช่องข้อมูลเป็นภาษาอังกฤษ ให้ทำตามธรรมเนียมนี้เวลาแก้ไฟล์เดิม อย่าเปลี่ยนไปใช้ชื่อภาษาอังกฤษ

### โครงสร้างข้อมูล (Firestore)

โฟลเดอร์ (collection) ต่าง ๆ (ดูสเปกหัวข้อ 5 สำหรับตารางช่องข้อมูลแบบเต็ม และกฎการแปลงชื่อตาราง→โฟลเดอร์ — ตารางแบบ snake_case กลายเป็นโฟลเดอร์แบบ camelCase เช่น `leave_requests` → `leaveRequests`):

```
users/{uid}            { name, email, role }              role ∈ employee | manager | hr
leaveTypes/{id}         { name }
leaveRequests/{id}      { title, reason, status, startDate, endDate, createdAt,
                          requesterId, requesterName,   ← จดชื่อซ้ำไว้ (denormalize)
                          approverId,  approverName,    ← จดชื่อซ้ำไว้ (denormalize)
                          leaveTypeId, leaveTypeName }   ← จดชื่อซ้ำไว้ (denormalize)
  leaveRequests/{id}/approvals/{id}  { authorId, authorName, message, createdAt }
```

**รายชื่อโฟลเดอร์ (collection) ทั้งหมดบน Firestore มีแค่ 4 อันนี้ — ห้ามสร้างโฟลเดอร์อื่นเพิ่มเองโดยไม่มีคนสั่ง:**
- `users` — ผู้ใช้ (ระดับบนสุด)
- `leaveTypes` — ประเภทการลา (ระดับบนสุด)
- `leaveRequests` — ใบขอลา (ระดับบนสุด)
- `leaveRequests/{id}/approvals` — ความเห็นการอนุมัติ (โฟลเดอร์ย่อยที่ซ้อนอยู่ในใบลาแต่ละใบ ไม่ใช่โฟลเดอร์กลาง)

ชื่อช่องข้อมูลและตัวพิมพ์เล็ก-ใหญ่ต้องตรงเป๊ะ มีผลกับระบบจริง — `status` กับ `Status` คือคนละช่องกันบน Firestore และจะทำให้ระบบพังแบบเงียบ ๆ ไม่ใช่แค่พิมพ์ผิดที่ตัว language server จะจับได้ ช่อง `*Name` ที่จดซ้ำไว้มีเพราะ Firestore ไม่มี JOIN เวลาเขียนโค้ดที่สร้าง/แก้ไฟล์ใน `leaveRequests` ต้องรักษาคู่ `Id`/`Name` ให้ตรงกันเสมอ

### สถานะใบลา 3 แบบ

ช่อง `status` ของใบลาใช้ได้แค่ 3 ค่านี้เท่านั้น (สเปกหัวข้อ 6):

1. `รอพิจารณา` (pending) — สถานะเริ่มต้นของใบลาที่สร้างใหม่ทุกใบ ระบบตั้งให้อัตโนมัติ ผู้ใช้เลือกเองไม่ได้
2. `อนุมัติ` (approved) — สถานะปลายทาง เปลี่ยนต่อไม่ได้อีก
3. `ไม่อนุมัติ` (rejected) — สถานะปลายทาง เปลี่ยนต่อไม่ได้อีก · ต้องมีความเห็นใน `approvals` อย่างน้อย 1 รายการอยู่ก่อนแล้วจึงจะเปลี่ยนเป็นค่านี้ได้

เปลี่ยนได้ตามลูกศร `รอพิจารณา → อนุมัติ` หรือ `รอพิจารณา → ไม่อนุมัติ` เท่านั้น ห้ามย้อนกลับ และการเปลี่ยนสถานะต้องแก้เฉพาะช่อง `status` เท่านั้น ห้ามเขียนทับช่องอื่นในไฟล์เดิม

### บทบาทผู้ใช้

`employee` / `manager` / `hr` (รหัสภาษาอังกฤษเก็บในช่อง `role` ส่วนป้ายภาษาไทยใช้แสดงบนหน้าจอ) — ดูตารางสิทธิ์แบบเต็มที่สเปกหัวข้อ 2 การบังคับสิทธิ์ตามบทบาทยังไม่ทำงานจริง (จะมาพร้อม Security Rules ในสัปดาห์ที่ 8) อย่าเพิ่งเขียนโค้ดกั้นสิทธิ์ตามบทบาทก่อนถึงตอนนั้น เว้นแต่จะมีคนสั่งให้ทำ

## ข้อห้ามสำคัญ: ห้ามใส่คีย์ลงไฟล์ที่ push

**ห้ามใส่ API key, secret, token หรือข้อมูลลับอื่นใดลงในไฟล์ที่จะถูก push ขึ้น GitHub** ก่อน commit หรือ push ทุกครั้ง ให้ตรวจเนื้อหาไฟล์ที่ staged จริง ๆ ด้วยตา ไม่ใช่แค่ดูชื่อไฟล์ (ไฟล์ชื่อดูไม่มีพิษภัยก็อาจมีคีย์แอบอยู่ในเนื้อหาได้)

ข้อควรรู้เฉพาะโปรเจกต์นี้:
- `js/firebase-config.js` มี `apiKey` ของเว็บแอปฝัง (hardcode) อยู่ตรง ๆ — **อันนี้ push ได้ตามปกติ** เพราะเป็น apiKey ฝั่งเว็บของ Firebase ไม่ใช่ความลับ (ความปลอดภัยจริงมาจาก Firestore Security Rules ไม่ใช่การซ่อน apiKey นี้) — แต่ห้ามสับสนกับคีย์ประเภทอื่น เช่น service-account key, admin SDK credential, หรือคีย์ของบริการภายนอก (เช่น OpenRouter ที่จะมาสัปดาห์ที่ 8) ซึ่งต้องไม่ push เด็ดขาด
- `.gitignore` ปัจจุบันกันไว้แค่ไฟล์ขยะที่เครื่องมือสร้างเอง (`node_modules/`, `.DS_Store` ฯลฯ) ยังไม่มีการกันไฟล์คีย์ — งานเพิ่ม `.gitignore` สำหรับไฟล์ที่มีคีย์จริงเป็นงานของสัปดาห์ที่ 7 เป็นต้นไป
- ก่อน push ทุกครั้ง ให้แสดงรายการไฟล์ที่กำลังจะ push ให้ผู้ใช้ตรวจสอบด้วยตาก่อนเสมอ ถ้าเจอไฟล์ที่มีคำว่า key / secret / credential / config ที่ไม่ใช่ `firebase-config.js` ให้หยุดและถามก่อน
